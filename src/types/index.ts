export type MenuItem = { id: string; label: string; path: string; order: number; visible: boolean };
export type Banner = { id: string; title: string; subtitle: string; image: string; ctaLabel: string; ctaLink: string; visible: boolean };
export type Collection = { id: string; slug: string; title: string; season: string; summary: string; cover: string; visible: boolean };
export type GalleryItem = { id: string; image: string; caption: string; visible: boolean };
export type Faq = { id: string; question: string; answer: string; visible: boolean };
